namespace HyperWhisper.Views.Controls;

/// <summary>
/// The main window's status bar. See the XAML header for why it is a control of
/// its own rather than a Grid inside MainWindow.xaml.
/// </summary>
public partial class StatusBarView : WpfUserControl
{
    public StatusBarView()
    {
        InitializeComponent();
    }
}
