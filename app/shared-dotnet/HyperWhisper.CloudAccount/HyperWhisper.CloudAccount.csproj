<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../HyperWhisper.Platform.Abstractions/HyperWhisper.Platform.Abstractions.csproj" />
    <!-- The account verdict, its cache and the offline grace live in the
         hw-license Rust core, reached through the one compiling copy of the
         generated binding (issue #290). -->
    <ProjectReference Include="../HyperWhisper.SharedCore/HyperWhisper.SharedCore.csproj" />
  </ItemGroup>
</Project>
