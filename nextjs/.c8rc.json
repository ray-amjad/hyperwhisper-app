{
  "//": "Line coverage for the server-side code. `all: true` is the point of this file: node --experimental-test-coverage only reports files a test loaded, so it printed 98.83% while lib/services/stripe-webhook.ts sat at 0% and never appeared in the table. c8 reads every file in `include` whether a test touched it or not.",
  "all": true,
  "src": ["."],
  "extension": [".ts"],
  "include": [
    "app/api/**/*.ts",
    "lib/**/*.ts",
    "server/**/*.ts",
    "src/lib/**/*.ts"
  ],
  "exclude": [
    "**/*.test.ts",
    "**/*.d.ts",
    "lib/disposable_domains.ts"
  ],
  "exclude-after-remap": true,
  "reporter": ["text"],
  "check-coverage": false
}
